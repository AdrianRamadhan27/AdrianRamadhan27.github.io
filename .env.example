# Copy this file to .env for local development (never commit .env itself).
# In production these are injected by .github/workflows/deploy.yml from
# GitHub Actions secrets of the same name (without the VITE_ prefix).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_ACCESS_TOKEN=
