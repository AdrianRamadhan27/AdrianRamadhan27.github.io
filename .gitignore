# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.seed

# One-time seed images (recovered from the old site) and the local key
# needed to upload them -- not part of the deployed app.
supabase/seed-assets

# Supabase CLI local state (project ref, pooler URL) -- not app config
supabase/.temp
